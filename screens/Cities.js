import React from 'react';
import { View, Text } from 'react-native';

import Header from '../components/header';

const Cities = (props) => {
    console.log("[cities]");
    return (
        <Header title="Cities"></Header>
    )
}

export default Cities;